import React from 'react'

const GreenBtn = ({text}) => {
  return (
    <div className='green_btn '>
      <span className=' montserrat-semi-bold text-sm'>{text}</span>
    </div>
  )
}

export default GreenBtn
